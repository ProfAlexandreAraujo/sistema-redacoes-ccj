# Renomeie este arquivo para "secrets.toml" para uso local com chave embutida.
# NUNCA envie o secrets.toml para o GitHub — ele já está no .gitignore.
#
# Para o Streamlit Community Cloud, configure este segredo
# no painel do app em: App Settings > Secrets

ANTHROPIC_API_KEY = "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx"
